// モーダルの書き換え
$('[data-model_name="tag"]').filter('[data-id="<%= @tag.id %>"]').remove();

// すでに表示されているgoalsを書き換え
$('#roomsIndex').html('<%= j(render partial: "rooms/room", collection: rooms, as: :room) %>')