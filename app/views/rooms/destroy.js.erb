// modalを閉じる処理
$("#roomModal").modal("hide");
$("body").removeClass("modal-open");
$(".modal-backdrop").remove();

// 画面の書き換え
$('[data-model_name="room"]').filter('[data-id="<%= @room.id %>"]').remove();