// モーダル追加
$("#modalArea").html(
  "<%= j(render 'form', chat: @chat, room: @room) %>"
);

// フォームにフォーカス
$("#modalArea").find(".modal").on('shown.bs.modal', () => {
  $("#modalArea").find('form [name="chat[content]"]').eq(0).focus();
})

// モーダル表示
$("#modalArea").find(".modal").modal("show");