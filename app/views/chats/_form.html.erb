
<%= form_with(model: chat, url: [room, chat], remote: true, html: {name: "chatForm", class: "p-2"} ) do |form| %>
  <% if chat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(chat.errors.count, "error") %> prohibited this chat from being saved:</h2>
      <ul>
        <% chat.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  
    <div class="field">
     <%= form.text_field :name, :placeholder => "名前"  %>
     
     <%= form.text_area :content, :placeholder => "コメント" %>
     <%= form.label "画像をアップロード" %><br><%= form.file_field :image %></br>
     </div>
    
    
    

  <div class="actions text-right">
    <%= link_to "閉じる", new_room_chat_path(room), class: "btn btn-secondary", remote: true %>
    <%= form.submit "投稿する" %>
  </div>
<% end %>
